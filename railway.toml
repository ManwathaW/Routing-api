[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "" # Uses the default ENTRYPOINT from the openrouteservice Docker image
restartPolicyType = "on_failure"

[env]
XMS = "1g"
XMX = "3g"
PORT = "8082"
CONTAINER_LOG_LEVEL = "INFO"
